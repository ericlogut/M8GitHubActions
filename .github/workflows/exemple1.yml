name: Exemple 1. Add to project

on:
  issues:
    types:
      - opened

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@RELEASE_VERSION
        with:
          # You can target a project in a different organization
          # to the issue
          project-url: https://github.com/users/ericlogut/projects/1
          github-token: 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
          labeled: bug, needs-triage
          label-operator: OR